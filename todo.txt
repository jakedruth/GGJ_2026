[ ] Comment all existing code

[ ] update the StatBlock object
	[x] allow adding stats together for a new stat object (have a limit?)
	[x] get the value of a stat
	[x] set the value of the stat
	[ ] generate a unique stat for a character. Will stats carry over otherwise if edited in gameplay??

[x] Create a task object
	[x] set up variables
		required stats, narration, description (with keywords)

[ ] Create Reward object
	[x] holds the rewards like gold, exp, new jobs, etc
	[ ] intergrate the claim funciton

[ ] Create a job object - This is an object the holds and handles a task instance
	[x] set up varialbes:
		task, active timer, location, number of thieves, rewards (Gold, Exp, new jobs), time job is added to the available job pool (???)
	[x] Create the states: not active (not started / hidden) -> active -> initiated -> in progress -> summary -> end
		A job can move from active to end (maybe summary) if the timer runs out
	[ ] Handle each state
		[x] not active
		[x] active
		[ ] initiated
		[ ] in progress
		[ ] summary
		[ ] end

[ ] Create the job system
	[x] have a list of all jobs
	[x] have a pool of available jobs
	[ ] have a job pop up on screen with a timer before it expires
	[ ] have a job display the narration and description
	[ ] allow assigning thieves to the jobs
	[ ] handle thieves going to the job, working on the job, then returning to the guild.
	[ ] handle a report screen

[ ] Make a thief object (if this is only a holder for stats, then this might not be needed)
	[ ] Set up varialbes
		stats

[ ] Make an abilities object
	Does this hold the art?
	[ ] Figure out what this is, how it works, etc
	[ ] this one gets a bit harder to manage

[ ] Make a character - holds and manages a thief instance and abilities
	[ ] name, level, experience, stat points, unlocked abilities, art/ mask
	[ ] handle if they are busy
	[ ] handle damage
	[ ] handle gold

[ ] Create a Team Manager
	[ ] hold and handles the thieves
	[ ] add and remove thieves
	[ ] handle input on selecting a theif

[ ] Create a day system
	manages everything else
	[ ] hiring thieves 

[ ] Make a few thieves
[ ] Make a few jobs
[ ] Make one day system